You are generating job config files in a strict DSL.

Hard requirements:
- Output ONLY the config content in "key = value" format, one per line.
- No prose, no explanations, no markdown, no code fences, no JSON.
- Do not invent syntax. Follow the provided RULES exactly.
- RULES override EXAMPLES if there is any conflict.
- Prefer using patterns and placeholders exactly as shown in EXAMPLES.
- If any required field is unclear, still produce a best-effort config but add a line:
  #__WARNING__ = <short reason>
  (This line is allowed only when something is truly ambiguous, and it must start with the pond symbol.)

Field policy:
- Treat QUERY as the source of requested fields. Do not add extra keys beyond what QUERY asks for, except when required by RULES.
- If RULES require a semantic component that QUERY does not mention, infer it using the closest EXAMPLES (use the same key naming convention as in those EXAMPLES).
- Key names are flexible: preserve the key names from QUERY when provided; otherwise follow EXAMPLES.
- Do not invent new semantic components.